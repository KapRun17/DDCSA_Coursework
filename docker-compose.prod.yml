services:
  db:
    image: postgres:17-alpine
    container_name: ddcsa-coursework-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER is required}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB is required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  server:
    image: ${SERVER_IMAGE:-ghcr.io/kaprun17/ddcsa-coursework-server:latest}
    container_name: ddcsa-coursework-server
    depends_on:
      db:
        condition: service_healthy
    environment:
      PORT: 5000
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL is required}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      NODE_ENV: production
    command: sh -c "npx prisma db push && npm run prisma:seed && npm start"

  client:
    image: ${CLIENT_IMAGE:-ghcr.io/kaprun17/ddcsa-coursework-client:latest}
    container_name: ddcsa-coursework-client
    depends_on:
      - server
    ports:
      - "80:80"

volumes:
  postgres_data:
